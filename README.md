# MockService for Angular

Service for Angular, which intercept http-requests and mock's responses

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.
